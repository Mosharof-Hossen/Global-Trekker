import { useEffect } from "react";
import { useLoaderData } from "react-router-dom";


const MyList = () => {
    useEffect(()=>{
        fetch("")
    },[])
    return (
        <div>
            My List
        </div>
    );
};

export default MyList;